import { PopulationFilterPipe } from './population-filter.pipe';

describe('PopulationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PopulationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
